import { StyleSheet } from "react-native";

export default StyleSheet.create({

  portfolioSelect: {
    width: "100%"
    // width: "90%",
    // position: "absolute",
    // top: 0,
    // zIndex: 999,
    // opacity: 1
  }

});