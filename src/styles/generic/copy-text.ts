import { StyleSheet } from "react-native";

export default StyleSheet.create({

  copyText: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between"
  }

});